This directory is deliberately empty of update files.

browser_whats_new_page_nimbus_no_update.js points app.update.altUpdateDirPath
here so that no update record exists, reproducing installs that are updated
outside of the Mozilla updater (MSIX, Snap, Flatpak and distro packages). This
placeholder only exists so that the directory itself is created.
