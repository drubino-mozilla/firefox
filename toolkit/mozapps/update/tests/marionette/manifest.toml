[DEFAULT]
subsuite = "integration"
tags = "os_integration"

["test_no_window_update_restart.py"]
run-if = [
  "os == 'mac'",
  "os == 'win'",
]
reason = "Only mac, and Windows in background mode, can be left running with no windows open"
